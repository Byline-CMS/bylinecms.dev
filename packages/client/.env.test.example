# Copy this file to .env.test. Integration tests connect to the dedicated
# test database (byline_test); the bootstrap asserts the DB name ends in
# `_test` and refuses to run otherwise.
POSTGRES_CONNECTION_STRING=postgres://byline:byline@127.0.0.1:5432/byline_test
