---
'@byline/richtext-lexical': patch
---

refresh the inline-image editor preview (src, width and height) from the resolved upload document, selecting the same position-aware variant the picker uses, and leaving all three untouched when the target, image or URL is unavailable
